<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- For Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"
        tools:targetApi="33" />

    <!-- Override Firebase minSdk requirement -->
    <uses-sdk
        tools:overrideLibrary="com.google.firebase.auth, com.google.firebase.database, com.google.firebase.storage, com.google.firebase.firestore" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/plant_illust"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HomePage"
        android:usesCleartextTraffic="true">

        <!-- LAUNCHER ACTIVITY (Only one should have MAIN/LAUNCHER intent) -->
        <activity
            android:name=".Welcome"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activities -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".menupage"
            android:exported="false" />

        <!-- Authentication Activities -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ForgotPassword"
            android:exported="false" />

        <activity
            android:name=".Authentication"
            android:exported="false" />

        <!-- Feature Activities -->
        <activity
            android:name=".CamActive"
            android:exported="false" />

        <activity
            android:name=".new_cam_page"
            android:exported="false" />

        <activity
            android:name=".pest_Detection"
            android:exported="false" />

        <!-- Info Activities -->
        <activity
            android:name=".ContactusActivity"
            android:exported="false" />

        <activity
            android:name=".AboutActivity"
            android:exported="false" />

    </application>

</manifest>